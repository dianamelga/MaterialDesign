<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="com.example.diana.materialdesign.MainActivity">

    <include
        android:id="@+id/mitoolbar"
        layout="@layout/toolbar"
        ></include>

    <!--todos los elementos que esten en un FrameLayout estaran uno en cima de otro-->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <TextView
            android:id="@+id/titulo"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Hello World!" />

        <android.support.design.widget.FloatingActionButton
            android:layout_height="wrap_content"
            android:layout_width="wrap_content"
            android:layout_gravity="bottom|right"
            android:layout_marginRight="@dimen/margen_boton"
            android:layout_marginBottom="@dimen/margen_boton"
            android:src="@drawable/ic_action_name"
            app:elevation="@dimen/elevacion_boton"
            app:fabSize="normal"
            app:borderWidth="0dp"
            android:id="@+id/fab"
            />

    </FrameLayout>

</LinearLayout>
